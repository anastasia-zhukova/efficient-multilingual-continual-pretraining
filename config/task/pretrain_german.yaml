task_name: pretrain_german

mlm_probability: 0.15

model:
  bert_model_name: "deepset/gbert-base"

dataloader:
  num_workers: 4
  pin_memory: true
  batch_size: 6

optimizer:
  lr: 0.0000005

n_epochs: 15
save_path: "model_weights/deepset_gbert_base_pretrained_100.pth"